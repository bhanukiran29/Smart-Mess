<?xml version="1.0" encoding="utf-8"?>
<!--
    item_attendance.xml
    Row layout for a single past attendance record in the RecyclerView.
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginBottom="10dp"
    app:cardCornerRadius="14dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="#E2E8F0"
    app:cardBackgroundColor="#FFFFFF">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="14dp">

        <!-- Coloured status bar on the left -->
        <View
            android:id="@+id/viewAttendanceBar"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:background="#3B82F6"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Meal icon -->
        <TextView
            android:id="@+id/tvAttMealIcon"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:gravity="center"
            android:textSize="22sp"
            android:text="🍽️"
            app:layout_constraintStart_toEndOf="@id/viewAttendanceBar"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginStart="10dp" />

        <!-- Day label + meal type -->
        <TextView
            android:id="@+id/tvAttDayLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Mon, May 26"
            android:textColor="#0F172A"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/tvAttMealIcon"
            app:layout_constraintEnd_toStartOf="@id/tvAttBadge"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="8dp" />

        <TextView
            android:id="@+id/tvAttMealType"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Lunch"
            android:textColor="#64748B"
            android:textSize="12sp"
            app:layout_constraintStart_toEndOf="@id/tvAttMealIcon"
            app:layout_constraintEnd_toStartOf="@id/tvAttBadge"
            app:layout_constraintTop_toBottomOf="@id/tvAttDayLabel"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="2dp"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Attended / Walk-in badge -->
        <TextView
            android:id="@+id/tvAttBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Attended"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:background="@drawable/bg_badge_blue"
            android:paddingHorizontal="10dp"
            android:paddingVertical="4dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
